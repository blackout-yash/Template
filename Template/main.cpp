#include <map>
#include <set>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;
#define int long long

#ifndef ONLINE_JUDGE
#include <bits/debug.h>
#else
#define debug(...)
#endif

void blackout_yash() {
    
}

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t = 1;
    // cin >> t;
    while(t--) blackout_yash();
    return 0;
}